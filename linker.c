#include <stdio.h>
#include <stdint.h>
#include <windows.h>

void resolve_imports() {
    // No imports needed for stub
}

uint64_t get_import_address(const char* dll, const char* func) {
    return 0;
}